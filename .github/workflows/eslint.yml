name: ESLint

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  schedule:
    - cron: "0 0 * * 1"

permissions:
  contents: read
  security-events: write
  actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status

jobs:
  call-eslint-workflow:
    uses: Mephisto5558/Mephisto5558/.github/workflows/eslint.yml@main
